{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "mimic-data-extraction": {
      "type": "stdio",
      "command": "python3",
      "args": ["${PLUGIN_ROOT}/scripts/launch_mcp.py"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
