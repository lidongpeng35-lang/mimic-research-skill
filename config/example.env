# Copy to a local, ignored file and fill values for an authorized read-only MIMIC database.
PGHOST=localhost
PGPORT=5432
PGDATABASE=mimiciv
PGUSER=readonly_user
PGPASSWORD=

# Optional state/export locations. Keep patient-derived exports outside the repository.
MIMIC_CONTRACT_DIR=
MIMIC_EXPORT_DIR=
